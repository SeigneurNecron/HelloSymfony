{% set navItem = {tooltip: navItem.title, style: ''}|merge(navItem) %}

<a class="navbar-link {{ app.current_route starts with (navItem.route|split('_')[0]) ? 'state-currentRoute' : '' }} {{ navItem.style }}"
   href="{{ path(navItem.route) }}" title="{{ navItem.tooltip }}">{{ navItem.title }}</a>