{% extends 'Include/Link.html.twig' %}

{% set linkInfo = {allowSameRoute: true}|merge(linkInfo) %}

{% block linkStyle %}
    navbar-link{{ app.current_route starts with (linkInfo.route|split('_')[0]) ? ' state-currentRoute' : '' }}{{ ' ' ~ parent() -}}
{% endblock %}