<nav class="navbar bg-primary navbar-expand-lg">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('Main_Home') }}" title="Hello Symfony">Hello Symfony</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav">
                {% set navItems = [
                    {route: 'Main_Home', title: 'Home'},
                    {route: 'Character_List', title: 'Characters'},
                    {route: 'Element_List', title: 'Elements'},
                    {route: 'WeaponCategory_List', title: 'Weapon Categories'},
                    {route: 'Region_List', title: 'Regions'},
                ] %}
                {% for navItem in navItems %}
                    {% include 'Include/NavItem.html.twig' with {'navItem': navItem} %}
                {% endfor %}
            </ul>
        </div>
    </div>
</nav>